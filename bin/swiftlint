set -e

modifiedFiles=$(git status | grep 'modified:' | wc -l)

repo_root=$( cd "$(dirname "${BASH_SOURCE[0]}")/" ; pwd -P ) 
swiftlint --fix --config $repo_root/../.swiftlint.yml $repo_root/../truelayer-payments-sdk-demo/
